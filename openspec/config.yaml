# OpenSpec Configuration — SGTA (Sistema de Gestión de Tutorías Académicas)
# Auto-generated by Gentle AI SDD Init
# Date: 2026-07-03

project:
  name: SGTA
  full_name: Sistema de Gestión de Tutorías Académicas
  description: Plataforma para gestionar tutorías académicas con enfoque en seguridad informática
  language: es

stack:
  language: Python 3.14
  framework: Flask
  database: SQLite
  orm: Flask-SQLAlchemy
  frontend: Bootstrap / Tailwind CSS
  forms: WTForms

security:
  auth: Flask-Login
  password_hashing: Flask-Bcrypt
  headers: Flask-Talisman (HTTPS, CSP, XSS, Clickjacking)
  audit: AuditLog table (usuario_id, acción, tabla, timestamp, IP)

testing:
  test_runner: null
  strict_tdd: false
  note: "No test runner detected. Install pytest to enable Strict TDD."
  linter: null
  formatter: null

persistence:
  mode: hybrid
  artifact_store: both
  engram: true
  openspec: true

roles:
  - name: estudiante
    permissions: [ver_horarios, solicitar_tutoria, ver_mis_tutorias, cancelar_tutoria]
  - name: docente
    permissions: [gestionar_disponibilidad, ver_estudiantes_agendados, registrar_feedback]
  - name: administrador
    permissions: [gestion_usuarios, asignar_roles, ver_auditoria]

changes:
  directory: openspec/changes/
